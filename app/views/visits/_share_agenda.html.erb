<% if @user.id %>  
  <%=form_tag( share_user_visits_path(@user) , method: :put ) do %>
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h3 id="shareAgendaModal">Share Your Weekly Agenda</h3>
    </div>

    <div class="modal-body">
      <p>Enter an email address with whom you want to share your agenda</p>
      <%= text_field_tag 'email', nil, placeholder: "Enter Email Address of Recipient" %>
    </div>

    <div class="modal-footer">
     <button class="btn btn-primary">Share Agenda</button>
    </div>
  <% end %> 
<% end %>  